server:
  port: 8082
  
  
spring:
  application:
    name: HOTEL-SERVICE
    
  datasource:
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:3308/microservicedb
    
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true 
    
  config:
    import: configserver:http://localhost:8085     
    
logging:
  config: classpath:log4j2.xml 
#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka
#  instance:
#    prefer-ip-address: true   